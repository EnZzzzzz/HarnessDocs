你按照这一份大纲实现后面的内容，整体上就还是嗯啊一个标题加卡片的形式，样式先统一用这一套样式。每一个一级标题的话，就是对应一页。然后里面的卡片内容我已经标注出来了，然后你还需要给我总结一下啊这页的大标题

# 为什么 Harness 热度这么高 基模厂商

卡片-1. 训练环境对 bench 结果有决定性作用。这里的例子就是我记得好像是Anthoripic还是OpenAI来着，他们发现训练时，如果能把query的环境全部setup，会大幅提升bench的分数
卡片-2. 训练上需要一个通用的底座，用来安装不同的插件。这里要举 GLM-5.3 训练的例子，它在训练阶段通过插件接入不同的环境，从而加强基座模型的能力

# 为什么 Harness 热度这么高 B 端 Harness 厂商

卡片-1. 豆包的成功证明了C端商业化路线的失败

卡片-2. 各个大厂都在押注 B端办公Harness，新的生态入口

# Harness关键词 -- 插件化 以DeepSeek Harness为代表，时空可组合性

备注：这一章节要读一下这篇论文：/Users/en/Documents/docs/deepseek-harness/Cordis论文全文翻译-时空可组合性编程范式.md。论文很长，可以让一个子Agent去读，然后由这个子Agent来实现这一章节

卡片-1. 时间可组合性
卡片-2. 空间可组合性

然后再帮我总结几个卡片，

# 为什么类vscode这种插件形式不行

这里你帮我总结几个卡片，你要读一下这个论文，看一下为什么VS Code不行

卡片-1.
卡片-2.

# Harness 关键词 -- 自进化 tibo 最新的访谈

这里的卡片你也帮我总结一下，要有几点。一，一个是，Tibo的这个主要观点就是会有一个通用的Harness。这个Harness它会作为底座，然后根据用户情使用情况，它会进化成自进化，它会学习用户的这个技能，然后给它自进化。第二个卡片就是Skill会被淘汰。至少要有这两个卡片，然后你可以看着再总结几个卡片

# Hermes 式的经验总结

这里的话卡片也由你来写，你要看一下Hermes它是怎么实现自我迭代的？怎么总结这个经验的

# 插件式 的 Function 拓展

这里可以以Deepseek Honeycomb为例吧。就是模型怎么去处理问题，然后并且去自己去开发一些给自己用的插件。这个是B端的场景，然后可以再举一些更贴近用户生活的一些例子，比如说买一个订票插件啊，或者是球赛插件啊等等

# Design Harness 怎么做

卡片-1. 双 Harness 架构
卡片-2. 识别场景

# 生成 Harness VS 判别 Harness

这里业界还没有一个通用的叫法，但是已经有相关的一些实践了，例如：https://www.anthropic.com/engineering/harness-design-long-running-apps、https://mp.weixin.qq.com/s/T_fBh7p82OHaKw75oq-5cQ

卡片-1. 生成 Harness 不太需要了解美学知识，只需要专注生成、关注工程可用性。极简模式，只包含必要的tool，可以产出纯净的训练数据
卡片-2. 判别 Harness 需要大量的视觉工具、需要注入大量的美学知识
卡片-3. 设计师必须介入。判别 Harness 的美学能力还无法做到完全自动化

# 关键产物

卡片-1 设计组件
卡片-2 场景SKILL
卡片-3 大量的训练数据

# 识别交付场景

卡片-1 开发态， 双Harness 共同存在
卡片-2 交付态， 三种不同的交付力度，最轻的一档就是交付Skill和设计组件，第二档的话就是交付生成Harness，并且针对场景去做增强和定制，第三档就是交付训练数据
